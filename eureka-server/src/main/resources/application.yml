eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8080

spring:
  application:
    name: eureka-server
  security:
    user:
      name: justk
      password: kong